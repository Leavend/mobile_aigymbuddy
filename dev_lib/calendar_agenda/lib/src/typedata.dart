/// Defines the format used when rendering weekday labels.
enum WeekDay { short, long }

/// Determines how the selected day should be aligned inside the header list.
enum SelectedDayPosition { left, right, center }

/// Scroll configuration for the full calendar modal.
enum FullCalendarScroll { horizontal, vertical }
